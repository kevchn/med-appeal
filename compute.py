from numpy import exp, cos, linspace
import os, time, glob

def unpack_to_set(textarea):
    return set(textarea.data.splitlines())

def generate_hello():
    """Returns hello"""
    results = "Hello"
    return(results)
